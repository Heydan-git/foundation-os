.env.local
.env.*.local
.env
.env.*
node_modules/
dist/
storybook-static/
storybook-static.zip
.DS_Store
.fos/
.fos-session.lock
.omc/state/
.omc/snapshots/*.md
!.omc/snapshots/.gitkeep
.claude/worktrees/
*.pyc
__pycache__/

# Secrets & credentials
*.pem
*.key
*.p12
*.pfx
id_rsa*
credentials.json
secrets.json
*.keystore

# Logs
*.log
modules/design-system/.env*

# ── WIKI / KNOWLEDGE LAYER (D-WIKI-01, 2026-04-15) ──
# Obsidian config locale (per user, non partagee)
.obsidian/
.obsidian.vimrc

# Modules Phase 5 : data brute + secrets + backtests raw
modules/*/data/
modules/*/secrets/
modules/*/backtests/raw/

# Manifest delta tracking claude-obsidian (regenerable)
.raw/.manifest.json

# ── Multi-session leftovers (cleanup 2026-04-20 reality-check) ──

# Dossier parallele mystere (duplicates trading indicators, origin inconnue, safe exclude)
.claire/

# Auto-memory Claude Code locale (duplique hors repo, pas pour commit)
.claude/projects/

# Dashboard routines Desktop (config user-specific port 3333, pas pour commit partage)
.claude/launch.json

# Build artifacts tokens Design System (regenerables, hors source)
modules/*/tokens/build/

# ── Donnees personnelles Cowork (emails, transactions, briefings quotidiens) ──
# Le shell HTML/MD est tracke, les DATA quotidiennes sont privees
docs/travaux-cowork/morning-intelligence/*.json
docs/travaux-cowork/morning-intelligence/*backup*.html
docs/travaux-cowork/2026-04-*-briefing-gmail/
docs/travaux-cowork/briefs-archives/
