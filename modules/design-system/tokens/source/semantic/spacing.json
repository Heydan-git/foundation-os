{
  "spacing": {
    "none": {
      "$value": "{space.scale.0}",
      "$type": "dimension"
    },
    "xs": {
      "$value": "{space.scale.1}",
      "$type": "dimension",
      "$description": "4px — tight inset, micro gaps"
    },
    "sm": {
      "$value": "{space.scale.2}",
      "$type": "dimension",
      "$description": "8px — compact UI"
    },
    "md": {
      "$value": "{space.scale.4}",
      "$type": "dimension",
      "$description": "16px — default inset/gap"
    },
    "lg": {
      "$value": "{space.scale.6}",
      "$type": "dimension",
      "$description": "24px — comfortable"
    },
    "xl": {
      "$value": "{space.scale.8}",
      "$type": "dimension",
      "$description": "32px — airy card padding"
    },
    "2xl": {
      "$value": "{space.scale.12}",
      "$type": "dimension",
      "$description": "48px — section separator"
    },
    "3xl": {
      "$value": "{space.scale.16}",
      "$type": "dimension",
      "$description": "64px — hero layout"
    }
  }
}
